cron:
- description: send reminders
  url: /cron/reminders
  schedule: every 1 hours synchronized

- description: check auto-expirations
  url: /cron/autoexpire
  schedule: every 5 minutes synchronized

